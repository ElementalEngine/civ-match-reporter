# ✅ Version tag for API (shown in /docs)
CIV_SAVE_PARSER_VERSION=dev

# ✅ MongoDB connection (use mongodb:// or mongodb+srv://)
MONGO_URI=
# 🟢 Database name
MONGO_DB=
# ⚠️ Timeouts / pooling
MONGODB_TIMEOUT_MS=5000
MONGODB_MAX_POOL_SIZE=100
MONGODB_MIN_POOL_SIZE=0

# 🟢 API bind
API_HOST=0.0.0.0
API_PORT=8000

# 🟢 CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost: